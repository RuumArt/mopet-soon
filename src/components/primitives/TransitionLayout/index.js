'use client';

import * as React from 'react';

import PropTypes from 'prop-types';

export const TransitionLayout = ({ children, router }) => {
  return children;
};

TransitionLayout.propTypes = {
  children: PropTypes.any,
};
